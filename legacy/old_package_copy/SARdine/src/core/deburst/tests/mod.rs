mod ew_deburst_tests;
mod geometry_tests;
// Temporarily disabled: depends on deburst helpers not currently exposed.
// Re-enable once compute_pairwise_weights/overlap_weight/valid_window/w_cos2 are wired.
// mod iw_deburst_tests;
